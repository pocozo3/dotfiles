################################################################################
#
# zshrc.linux Linux(Debian)環境設定
#     ~/.zsh.d/zshrc.linux
#     対話的起動時のみの設定
#
################################################################################

# terminal の種類によって色の変更などを行う
case $TERM in
    linux*)
        alias tmux='TERM=fbterm tmux -2'
        fbterm
        ;;
    *)
        ;;
esac
